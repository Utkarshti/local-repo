#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
int main(){
    vector<int> arr;
    arr.push_back(1);
    arr.push_back(4);
    arr.push_back(4);
    arr.push_back(4);
    arr.push_back(5);
    arr.push_back(3);
    int n=arr.size();
    vector<int> vec1;
   sort(arr.begin(),arr.end());
     int i=0;
     for(int )
    return 0;
}